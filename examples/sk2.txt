(block
    (store 0 0)
    (store 1 1)
    (store i i)
    (del 1)
    (if (ast?? 1) 
        (if (== (contains 2) #f)
                (block (store i (* 2 i))
                        (del 2))
                (del (- i 1)))
        (block (store 0 10)
                (get 0))))